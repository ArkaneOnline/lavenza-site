<%- include("partials/header"); -%>

    <%- include("partials/navbar"); -%>
        <center>
            <img class="logo" src="/assets/logo-circular.png">
            <div class="centerText">
                <h1>Lavenza</h1>
                <p>The first ever, public Persona Compendium Bot!</p>
            </div>
            <div class="inviteButton">
                <% if(locals.username) { %>
                    <a class="button is-link is-rounded" href="/invite">Invite</a>
                <% } else { %>
                    <a class="button is-link is-rounded" href="/auth">Login to Invite</a>
                <% } %>
            </div>
        <%- include("partials/footer"); -%>